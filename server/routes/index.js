const user = require('./user'); 
const  event = require('./event');
const auth = require('./auth');
const coms = require('./coms');
module.exports = {
    User: user,
    Event: event,
    Auth: auth,
    Coms: coms
}